[project]
name = "handyman"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fire>=0.7.1",
    "pandas>=2.3.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.17.0",
]
